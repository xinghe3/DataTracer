<UserControl x:Class="CN.MACH.Aop.DataTracer.Views.Redis.RedisSubscribView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:common="clr-namespace:CN.MACH.AI.UI.Controls;assembly=CN.MACH.AI.UI.Controls.WPF"
             xmlns:local="clr-namespace:CN.MACH.Aop.DataTracer.Views.Redis"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid >
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="45" />
                <ColumnDefinition Width="45" />
                <ColumnDefinition Width="45" />
                <ColumnDefinition Width="45" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="searchKeyWords" KeyUp="searchKeyWords_KeyUp" >

            </TextBox>
            <Button Grid.Column="1" Click="Button_Click">
                <Path Data="M5,5 10,5 20,10 0,10" Fill="Aqua" />
            </Button>
            <Button Grid.Column="2" Click="LoadMsgs_Click" Content="Load Saved Messages"></Button>
            <Button Grid.Column="3" Click="ClearSavedMsgs_Click" Content="Clear Saved Messages"></Button>
            <CheckBox Grid.Column="4" IsChecked="{Binding Paused}" Content="暂停" Margin="5" VerticalAlignment="Center" />
        </Grid>
        <ListView x:Name="msglist" Grid.Row="1" ItemsSource="{Binding Records}" ItemTemplate="{DynamicResource RedisListDataTemplate}" Grid.RowSpan="2">
            <ListView.Resources>
                <DataTemplate x:Key="RedisListDataTemplate">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                            <ColumnDefinition />
                            <ColumnDefinition Width="150" />
                            <ColumnDefinition Width="200" />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <TextBlock Margin="5" VerticalAlignment="Center" Text="{Binding Time}" />
                        <Button Grid.Column="1" x:Name="SendMsg" Click="SendMsg_Click"  Margin="5" VerticalAlignment="Center" Content="Send" />
                        <Button Grid.Column="2" x:Name="SaveMsg" Click="SaveMsg_Click" Margin="5" VerticalAlignment="Center" Content="Save" />
                        <TextBlock Grid.Column="3"  Margin="5" VerticalAlignment="Center" Text="{Binding Name}" />
                        <TextBlock Grid.Column="4"  Margin="5" VerticalAlignment="Center" Text="{Binding Desc}" />
                        <common:JsonView Grid.Column="5" JsonString="{Binding Value}" />
                    </Grid>
                </DataTemplate>
            </ListView.Resources>

        </ListView>
    </Grid>
</UserControl>
